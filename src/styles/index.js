import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  formControl: {
    // margin: ".5rem",
    // minWidth: "12rem",
    // width: "100%",
  },
}));
