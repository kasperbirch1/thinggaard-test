import { createMuiTheme } from "@material-ui/core/styles";

export const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#6b339b",
      main: "#470082",
      dark: "#31005b",
      contrastText: "#fff",
    },
    secondary: {
      light: "#33bb4b",
      main: "#00aa1e",
      dark: "#007615",
      contrastText: "#fff",
    },
  },
});
